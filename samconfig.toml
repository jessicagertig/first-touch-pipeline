version = 0.1

# Secrets are passed via `make deploy` (sourced from .env). Do NOT put them here.

[default.deploy.parameters]
stack_name = "first-touch-slack"
region = "us-east-1"
profile = "polymer"
capabilities = "CAPABILITY_IAM"
resolve_s3 = true
disable_rollback = true
confirm_changeset = true
